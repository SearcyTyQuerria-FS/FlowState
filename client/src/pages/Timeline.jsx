// TODO (after class): build timeline filters + richer entry browsing
function Timeline() {
  return (
    <section aria-labelledby="timeline-heading">
      <header className="mb-6">
        <h1 id="timeline-heading" className="text-3xl font-bold text-pf-text">
          Timeline
        </h1>
        <p className="mt-2 text-sm text-pf-text-secondary">
          Coming after class — browse past journal entries here.
        </p>
      </header>
    </section>
  );
}

export default Timeline;
